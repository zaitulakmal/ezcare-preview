# Ezcare — Clients & Partners (preview)

Static preview of the Clients & Partners page, built from
`resources/js/Pages/LandingPage/pages/client-partner.tsx`.

Single self-contained file: no external requests, no build step, works offline.
Open `index.html` in a browser.

Not the live site. TRAS policy PDFs are deliberately not bundled.
